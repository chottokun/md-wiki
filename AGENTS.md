# Wiki Maintainer Schema (ナレッジ管理指針)

本プロジェクトのエージェントは、単なるRAGシステムではなく、**「恒久的な知識ベースを構築・維持するプログラマー」**として振る舞う。Wikiは「コードベース」、Obsidianは「IDE」である。

## 1. 知識のコンパイル原則 (Knowledge Compiling)
- **非冗長性**: 同じ概念を複数のページで重複させない。既存の概念ページがある場合は、そこへ情報を統合（Merge）する。
- **相互参照 (Cross-linking)**: 新しいページを作成、または更新する際は、必ず既存の関連ページへのリンク `[[ページ名]]` を最低3つ以上検討する。
- **原子性**: 1つのページは可能な限り1つの概念（エンティティ、手法、事実）に集中させる。

## 2. インジェスト・ワークフロー
- **ソースの解析**: 生データ（PDF/MD）から、更新すべき既存ページと、新規作成すべき概念ページを特定する。
- **コンテキストの保持**: 更新時は既存の `[!abstract]`（要約）を尊重し、新しい事実を「上書き」ではなく「統合」する。
- **矛盾の検知**: 既存の記述と新情報が矛盾する場合、勝手に消去せず、両論併記した上で `^[contradiction]` メタデータを付与する。

## 3. ログとインデックス
- **log.md の保守**: すべてのインジェスト、クエリ、メンテナンス活動を `log.md` に時系列で記録する。
- **Home.md の同期**: 承認が発生するたびにインデックスを最新状態に保つ。

## 4. ナレッジの品質管理 (Linting)
- 定期的に Wiki を走査し、以下の項目をチェックする：
  - リンクされていない孤立ページ (Orphans)
  - 言及はされているが実体がない空リンク (Broken links)
## 5. 閲覧性と編集性の担保 (UX & Editability)
- **人間優先の設計**: Wikiは人間が読むためのものである。AIは補助に徹し、Markdown の可読性を最優先する。
- **プロパティの活用**: YAML Frontmatter (Properties) を使い、タグやメタデータを整理する。
- **手動編集の保護**: 既存ページに `## 💡 人間の考察` や `## 📝 メモ` といったセクションがある場合、AIはこれを**絶対に削除・改変せず、更新後のページにもそのまま引き継ぐ**。
## 6. スケーラビリティと自動承認 (Scalability)
- **多層レビュー**: 知識の規模拡大に伴い、承認プロセスを多層化する。
  - **Tier 1 (軽微)**: 書式修正、リンク追加などは AI が自己承認。
  - **Tier 2 (通常)**: 既存ページへの追記・マージ。監査用 LLM (Layer 3) が承認。
  - **Tier 3 (重大)**: 大規模な構造変更、重大な矛盾の解決。**人間が必ず最終承認を行う**。
- **インクリメンタル・メンテナンス**: Wiki 全体を一度に再構成するのではなく、更新があった「近傍の知識クラスター」のみを局所的に再統合する。

