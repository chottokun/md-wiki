# Wiki Maintainer Schema (ナレッジ管理指針)

本プロジェクトのエージェントは、単なるRAGシステムではなく、**「恒久的な知識ベースを構築・維持するプログラマー」**として振る舞う。Wikiは「コードベース」、Obsidianは「IDE」である。

## 1. 知識のコンパイル原則 (Knowledge Compiling)
- **非冗長性**: 同じ概念を複数のページで重複させない。既存の概念ページがある場合は、そこへ情報を統合（Merge）する。
- **相互参照 (Cross-linking)**: 新しいページを作成、または更新する際は、必ず既存の関連ページへのリンク `[[ページ名]]` を最低3つ以上検討する。
- **原子性**: 1つのページは可能な限り1つの概念（エンティティ、手法、事実）に集中させる。

## 2. インジェスト・ワークフロー
- **ソースの解析**: 生データ（PDF/MD）から、更新すべき既存ページと、新規作成すべき概念ページを特定する。
- **コンテキストの保持**: 更新時は既存の `[!abstract]`（要約）を尊重し、新しい事実を「上書き」ではなく「統合」する。
- **矛盾の検知**: 既存の記述と新情報が矛盾する場合、勝手に消去せず、両論併記した上で `^[contradiction]` メタデータを付与する。

## 2.5. レビュー・ワークフロー (HITL)
- **一時保存の禁止**: `_staged` のような専用ディレクトリは使用せず、生成されたページは直接本番の `wiki/` フォルダに出力する。
- **未審査タグの付与**: AIが作成・更新したページには、必ずYAMLフロントマターに `tags: [未審査]` を付与する。
- **承認プロセス**: ユーザーが Obsidian 上で内容を確認し、手動で `未審査` タグを削除した瞬間を「承認」と見なし、本番の検索用データベース (Qdrant) への同期対象とする。

## 4. ナレッジの品質管理 (Linting)
- 定期的に Wiki を走査し、以下の項目をチェックする：
  - リンクされていない孤立ページ (Orphans)
  - 言及はされているが実体がない空リンク (Broken links)
## 5. 閲覧性と編集性の担保 (UX & Editability)
- **人間優先の設計**: Wikiは人間が読むためのものである。AIは補助に徹し、Markdown の可読性を最優先する。
- **プロパティの活用**: YAML Frontmatter (Properties) を使い、タグやメタデータを整理する。
- **メタデータの厳格管理**: プロパティは Pydantic スキーマ (`WikiFrontmatterSchema`) で定義された構造を厳守する。
  - **要約の品質**: `abstract`（要約）にはプレースホルダー（「自動生成スタブ」等）を禁止し、具体的かつ専門的な記述を求める。
  - **エイリアスの定義**: `aliases` は、ページタイトルの「完全な別名」または「略称」に限定する。関連用語や類似概念を混入させてはならない。
- **手動編集の保護**: 既存ページに `## 💡 人間の考察` や `## 📝 メモ` といったセクションがある場合、AIはこれを**絶対に削除・改変せず、更新後のページにもそのまま引き継ぐ**。
## 7. AI共同編集ルール (Co-Editing Rules)
LLMは「執筆案を作成するエディター」であり、最終判断は Git が行う。以下のルールを厳守すること：
1. **事実の保護**: 既存の事実は削除せず、新しい事実に矛盾がある場合は両論併記する。
2. **差分 (Diff) 指向**: 原則として全文を書き換えず、変更が必要な箇所のみを修正パッチ形式で提案する。
3. **重複の統合**: 複数のソースから同じ概念が発見された場合、既存の記述を強化する形で統合する。
4. **構造の維持**: 見出し構造 (#, ##) や Obsidian 特有のメタデータを破壊してはならない。
5. **更新判断**: 些末な変更（書式の微調整など）は「更新不要」と判断し、ノイズを削減する。

## 8. コンフリクト解決 (Conflict Resolution)
- Git の衝突マーカー (`<<<<<<<`, `=======`, `>>>>>>>`) を発見した場合、LLM は以下の「ピンポイント解決原則」に従う：
  1. **スコープの限定**: 衝突マーカーで囲まれた範囲内のみを修正対象とする。マーカー外の部分は**一字一句変えてはならない**。
  2. **意図の統合**: 両方の変更意図を解析し、一次情報の事実に最も即した統合案を作成する。
  3. **最小変更の法則**: 解決後のテキストは、可能な限り元の文脈の流れを維持し、不必要なリフレーズ（言い換え）を避ける。

